"""FEAGI Transport Adapters.

This module contains transport-specific adapters that provide access to FEAGI's
v1 API business logic through different transport protocols.

Each adapter translates transport-specific requests/responses to/from the
standardized v1 API format, ensuring identical behavior regardless of transport
protocol.
"""
