[workspace]
resolver = "2"
members = [
    "crates/feagi-types",
    "crates/feagi-burst-engine",
    "crates/feagi-plasticity",
    "crates/feagi-python",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Neuraville Inc. <feagi@neuraville.com>"]
license = "Apache-2.0"
repository = "https://github.com/feagi/feagi"

[workspace.dependencies]
# Core dependencies (shared across crates)
thiserror = "1.0.50"
rayon = "1.10"
ndarray = "0.16"
ahash = "0.8"  # Fast non-cryptographic hashing

# PyO3 for Python bindings
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"

[profile.release]
lto = true           # Link-time optimization
codegen-units = 1    # Better optimization, slower compile
opt-level = 3        # Maximum optimization
panic = "abort"      # Smaller binary, no unwinding

[profile.dev]
opt-level = 1        # Some optimization in dev for reasonable performance