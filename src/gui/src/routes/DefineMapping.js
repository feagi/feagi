import React from "react";
import Mapping from "../components/Mapping";

const DefineMapping = () => {
  return (
    <div>
      {/* <Mapping definedSensory={definedSensory} definedMotor={definedMotor} /> */}
      <Mapping />
    </div>
  );
};

export default DefineMapping;
