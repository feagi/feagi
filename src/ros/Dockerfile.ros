FROM tiryoh/ros2-desktop-vnc:foxy

SHELL ["/bin/bash", "-c"]

# install tools
RUN apt-get update && \
    apt-get install -y \
        xterm \
        python3-pip && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# set up workspaces and run
WORKDIR /home/ubuntu/
COPY setup_container.sh setup.py ros_laser_scan.py ros_teleop.py micro-ros_publisher/ HC_SR04_Foxy.py Sonar_reader1.py Sonar_reader.py ardiunotopython/ /home/ubuntu/
RUN chmod +x setup_container.sh && \
    ./setup_container.sh
